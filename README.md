# GradientGenerator
- Generate a gradient using the colors from the pickers and turn the color from the slider.  
- Use the gradient as the form background.
- has a 'Surprise Me' button which generates random gradients (and updates the inputs).
- Check for contrast between the gradient and the text.
- opacity slider
